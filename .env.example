# ============================================================
# Antigravity Auto - Config (.env)
# Salin file ini menjadi .env lalu isi nilai-nya
# JANGAN commit .env (berisi rahasia IP & password)
# ============================================================

# Dashboard 9Router - GANTI dengan IP:PORT server Anda
LOGIN_URL=http://YOUR_IP:PORT/login
TARGET_URL=http://YOUR_IP:PORT/dashboard/providers/antigravity
DASH_PASSWORD=YOUR_DASH_PASSWORD

# OAuth redirect (localhost -> IP server via route rewrite)
REDIRECT_FROM=http://localhost:20128
REDIRECT_TO=http://YOUR_IP:PORT

# Browser
USER_DATA_DIR=./browser_profile
RESET_PROFILE=false
HEADLESS=false
# true = tanpa tampilan (VPS), false = kelihatan (bisa handle verifikasi manual)

# Akun & log
ACCOUNTS_FILE=accounts.txt
URL_LOG_FILE=urls.txt

# Fix akun ke-2 (account chooser)
CLEAR_EACH=true
RESTART_BROWSER_PER_ACCOUNT=false

# Flask UI
FLASK_PORT=5000
FLASK_HOST=0.0.0.0
